# CHANGELOG

## 0.1.2 2026/09/03 @驳是

* FEAT Add `setup` in Fetcher
* FEAT Remove `normalizeErrorName`
* BREAK 默认输出的 `factory` 不在返回工厂方法，而是直接返回 `Fetcher` 实例

## 0.1.0 2026/09/02 @驳是

* FEAT First version as `@fetchx` family
